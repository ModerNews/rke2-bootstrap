#cloud-config

# virtio1 (vdb) → /var/log

disk_setup:
  /dev/vdb:
    table_type: "gpt"
    overwrite: true
    layout: true

fs_setup:
  - label: logs
    filesystem: "xfs"
    device: "/dev/vdb1"
    overwrite: false

mounts:
  - ["/dev/vdb1", "/var/log", "xfs", "defaults,nofail", "0", "2"]
